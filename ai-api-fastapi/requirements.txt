# 파일: requirements.txt
# 역할: Python 의존성 목록
# 사용: pip install -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pyyaml>=6.0
torch>=2.0.0 
transformers>=4.40.0
sentencepiece>=0.1.99    # ← tired_v5 토크나이저(슬로우→패스트 변환)에 필요
protobuf>=3.20.0         # ← sentencepiece .model 파싱에 필요(2차 실패 예방)
pytest>=8.0.0
httpx>=0.27.0
boto3>=1.34.0   # ← 이 줄 추가 (S3 다운로드용)